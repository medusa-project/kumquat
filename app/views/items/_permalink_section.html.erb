<%
  # Requires the following locals:
  # item [Item]
%>

<div class="media">
  <div class="media-left">
    <i class="fa fa-link"></i>
  </div>
  <div class="media-body">
    <h4 class="media-heading">Permalink
      <button class="btn btn-default btn-xs dl-copy-permalink"
              data-clipboard-text="<%= item_url(item) %>"
              type="button">
        <i class="fa fa-clipboard"></i> Copy
      </button>
    </h4>
    <%= item_url(item) %>
  </div>
</div>
