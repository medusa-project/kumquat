<% if @count > 0 %>
  <div class="row justify-content-md-center">
    <div class="col-md-auto">
      <%= paginate_items(@count, @limit, @current_page) %>
    </div>
  </div>

  <div class="dl-cards d-none d-sm-block d-md-none">
    <%= entities_as_cards(@items) %>
  </div>
  <div class="d-sm-none d-md-block">
    <%= entities_as_media(@items, show_collections: !@collection) %>
  </div>

  <div class="row justify-content-md-center">
    <div class="col-md-auto">
      <%= paginate_items(@count, @limit, @current_page) %>
    </div>
  </div>
<% else %>
  <div class="dl-no-results">
    <%= no_results_help(params[:q], @suggestions) %>
  </div>
<% end %>

<% if Rails.env.development? %>
  <%= render partial: 'shared/results_debug', locals: {
      es_request: @es_request_json,
      es_response: @es_response_json } %>
<% end %>
