$('#pt-items').html('<%= j(render partial: 'admin/items/items', locals: {
        items: @item_set.items,
        count: @count,
        start: @start,
        limit: @limit,
        current_page: @current_page
}) %>');

Application.view.attachEventListeners();
