<table class="table table-striped">
  <thead>
    <th style="width: 1px"></th>
    <th style="width: 1px"></th>
    <th>Name</th>
  </thead>
  <tbody>
    <% @agent_types.each do |type| %>
      <tr>
        <td>
          <button type="button" class="btn btn-default btn-xs dl-edit-agent-type"
                  data-toggle="modal" data-target="#dl-edit-agent-type-modal"
                  data-agent-type-id="<%= type.id %>">
            <i class="fa fa-pencil"></i> Edit
          </button>
        </td>
        <td>
          <%= link_to admin_agent_type_path(type),
                      class: 'btn btn-danger btn-xs', method: :delete,
                      data: { confirm: 'Are you sure you want to delete '\
                              'this agent type?' } do %>
            <i class="fa fa-trash"></i> Delete
          <% end %>
        </td>
        <td><%= type.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
