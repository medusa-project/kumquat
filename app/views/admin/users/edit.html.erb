<% provide(:title, "Edit #{@user.username}") %>
<% provide(:body_id, 'users_edit') %>
<% provide :active_nav, 'access' %>

<%= admin_breadcrumb({ label: 'Home', url: admin_root_path },
                     { label: 'Users', url: admin_users_path },
                     { label: @user.username, url: admin_user_path(@user) },
                     { label: 'Edit' }) %>

<h1>Edit <%= @user.username %></h1>

<%= render partial: 'form', locals: { user: @user, context: :edit } %>
