<table class="table table-striped">
  <thead>
    <tr>
      <th style="width:1%"></th>
      <th>NetID</th>
      <th>Type</th>
      <th>Role(s)</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td>
          <span class="badge <%= user.enabled ? 'badge-success' : 'badge-danger' %>">
            <%= user.enabled ? 'Enabled' : 'Disabled' %></span>
        </td>
        <td><%= link_to user.username, admin_user_path(user) %></td>
        <td><%= user.human ? 'Human' : 'Non-Human' %></td>
        <td>
          <ul>
            <% user.roles.each do |role| %>
              <li><%= link_to role.name, admin_role_path(role) %></li>
            <% end %>
          </ul>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
