- if object.errors.any?
  .panel.panel-danger.submission-error
    .panel-heading
      %h3.panel-title
        = pluralize(object.errors.count, 'error')
        prevented this #{object.class.to_s.underscore.humanize.downcase}
        from being saved:
    .card-body
      %ul
        - object.errors.full_messages.each do |msg|
          %li= msg
