<% @images.each do |image| %>
  <% url = binary_image_url(image,
                       region: :square,
                       size: 180,
                       cache: false) %>
  <%= image_tag(url) if url %>
<% end %>
